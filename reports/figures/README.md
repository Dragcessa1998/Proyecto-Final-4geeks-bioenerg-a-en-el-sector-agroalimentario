# Figuras del EDA

Las figuras del análisis exploratorio se generan automáticamente al ejecutar:

```bash
python src/run_pipeline.py
```

Los PNG resultantes quedan en esta carpeta y se ignoran en GitHub para evitar ruido de archivos generados.

